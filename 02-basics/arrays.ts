const superheroes: string[] = [];
// const numbers: number[] = [];
const numbers: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MLModels: number[][] = [[255, 34, 43], []];

superheroes.push("Superman");
numbers.push(2);

allUsers.push({
  name: "Gopal",
  isActive: true,
});

export {};
